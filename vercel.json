{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "rewrites": [
    {
      "source": "/api/viator/(.*)",
      "destination": "/api/viator/[...endpoint]?endpoint=$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
